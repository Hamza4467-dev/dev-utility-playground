import { DisableOnClickDirective } from './disable-on-click.directive';

describe('DisableOnClickDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableOnClickDirective();
    expect(directive).toBeTruthy();
  });
});
